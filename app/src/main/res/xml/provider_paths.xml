<paths>
    <external-path
        name="ascode_data"
        path=".AndroidSCode/" />
    <external-path
        name="ascode_backups"
        path=".AndroidSCode/backups/" />
    <!-- Signed APKs built by the unified compile dialog, so its "Install" button can hand the
         file to the package installer ("ascode/signed_apk"). -->
    <external-path
        name="ascode_signed_apk"
        path="ascode/signed_apk/" />
</paths>
